import { TratarImagemPipe } from './tratar-imagem.pipe';

describe('TratarImagemPipe', () => {
  it('create an instance', () => {
    const pipe = new TratarImagemPipe();
    expect(pipe).toBeTruthy();
  });
});
